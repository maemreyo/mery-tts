# Model corrupt or reinstall

Use this topic when an installed model fails checksum, size, or load validation.

1. Run `mery doctor` to confirm the corrupt model or artifact path category.
2. Reinstall the same model from the catalog instead of editing files manually.
3. Keep diagnostics export available if reinstall fails repeatedly.

Mery protects active models from cleanup commands and quarantines corrupt artifacts when possible.
