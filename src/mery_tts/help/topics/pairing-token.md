# Pairing and token recovery

Use this topic when a client cannot authenticate, the token is missing, or pairing expired.

1. Run `mery pair` on the local machine running Mery.
2. Copy the short pairing code into the client before it expires.
3. If a token was rotated, repeat pairing from the client that needs access.

Pairing is local to the helper and works fully offline.
