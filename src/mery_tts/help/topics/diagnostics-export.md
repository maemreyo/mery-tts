# Diagnostics export

Use this topic when support, QA, or an integration partner needs a local evidence bundle.

1. Run `mery doctor` first to capture current health.
2. Use diagnostics export from the CLI or Console when available.
3. Review exported files before sharing them outside the machine.

Diagnostics must not include raw input text, bearer tokens, private URLs, reference audio, or private filesystem paths.
